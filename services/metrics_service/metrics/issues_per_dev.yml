name: issues_per_dev
metric: >
  SELECT i.repo_id,
  u.id     AS developer_id,
  u.NAME   AS developer_name,
  Count(*) AS assigned_issues_count
  FROM   users u
  JOIN issues i
  ON u.id = i.assignee_id
  WHERE  i.repo_id = %s
  GROUP  BY u.id,
  u.NAME,
  i.repo_id;
